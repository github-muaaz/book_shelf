export default interface BookInterface {
        id: number;
        title: string;
        cover: string;
        pages: number;
        published: number;
        isbn: string;
        author: string;
}